<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom Styles -->
    <style>
        body {
            background-color: #f0f8ff;
            font-family: Arial, sans-serif;
        }
        .main-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column;
        }
        .btn-custom {
            width: 250px;
            margin: 15px;
            font-size: 18px;
            padding: 15px;
            border-radius: 10px;
            transition: transform 0.3s ease, background-color 0.3s ease;
        }
        .btn-custom:hover {
            transform: scale(1.05);
            background-color: #0062cc;
            color: white;
        }
        .btn-enquiry {
            background-color: #28a745;
            color: white;
        }
        .btn-student {
            background-color: #007bff;
            color: white;
        }
        .btn-office {
            background-color: #17a2b8;
            color: white;
        }
    </style>
</head>
<body>

    <div class="main-container">
        <h1 class="mb-4">Welcome to Our Portal</h1>
        <p class="mb-5">Select an option to get started:</p>

        <button class="btn btn-custom btn-enquiry" onclick="window.location.href='/index'">Enquiry</button>
        <button class="btn btn-custom btn-student" onclick="window.location.href='/student-login'">Student Login</button>
        <button class="btn btn-custom btn-office" onclick="window.location.href='/admin/login'">Office Login</button>
    </div>

    <!-- Bootstrap JS & Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>

</body>
</html>
